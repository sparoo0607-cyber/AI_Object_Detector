<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Camera Preview View (Full Screen) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/cameraPreview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:scaleType="fillCenter" />

    <!-- Custom Multi-Layer Canvas Overlay -->
    <com.accessibility.detector.ui.OverlayView
        android:id="@+id/overlayView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Top Accessible Navigation & AI Status Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="14dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- SAHEY Logo & Active AI Modules Badge -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/pill_background"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="14dp"
            android:paddingVertical="8dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SAHEY"
                android:textColor="@color/accent_green"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=" • AI Multimodal"
                android:textColor="@color/white"
                android:textSize="13sp" />
        </LinearLayout>

        <Space
            android:layout_width="8dp"
            android:layout_height="wrap_content" />

        <!-- Safety Shield Emergency Toggle Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSafetyShield"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:insetLeft="0dp"
            android:insetTop="0dp"
            android:insetRight="0dp"
            android:insetBottom="0dp"
            app:cornerRadius="24dp"
            app:backgroundTint="@color/surface_card"
            app:icon="@drawable/ic_shield"
            app:iconTint="@color/accent_red"
            app:iconGravity="textStart"
            app:strokeColor="@color/surface_border"
            app:strokeWidth="1dp"
            android:contentDescription="@string/action_safety" />

        <Space
            android:layout_width="8dp"
            android:layout_height="wrap_content" />

        <!-- Mute / Unmute Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnMuteToggle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:insetLeft="0dp"
            android:insetTop="0dp"
            android:insetRight="0dp"
            android:insetBottom="0dp"
            app:cornerRadius="24dp"
            app:backgroundTint="@color/accent_green"
            app:icon="@drawable/ic_speaker"
            app:iconTint="@color/black"
            app:iconGravity="textStart"
            android:contentDescription="@string/mute" />
    </LinearLayout>

    <!-- Bottom Floating Status Card & Quick Action Bar -->
    <LinearLayout
        android:id="@+id/bottomSection"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Live Announcement Status Card -->
        <LinearLayout
            android:id="@+id/bottomCard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/card_background"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvLiveLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/status_scanning"
                android:textColor="@color/accent_green"
                android:textSize="20sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvDetailText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="SAHEY is actively monitoring objects, hazards, sounds, text, and signs."
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

            <TextView
                android:id="@+id/tvActiveModules"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="AI Engines: Vision • Danger • OCR • Sound • Sign • Speech"
                android:textColor="@color/text_muted"
                android:textSize="11sp" />
        </LinearLayout>

        <!-- Quick Accessible Action Strip -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- Read Text Now Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnReadText"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:text="@string/action_read"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:textSize="13sp"
                    app:backgroundTint="@color/accent_yellow"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_book"
                    app:iconTint="@color/black" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <!-- Ask / Voice Command Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnListenSpeech"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:text="@string/action_listen"
                    android:textColor="@color/white"
                    android:textStyle="bold"
                    android:textSize="13sp"
                    app:backgroundTint="@color/accent_blue"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_mic"
                    app:iconTint="@color/white" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <!-- Translate Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnTranslate"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:text="@string/action_translate"
                    android:textColor="@color/white"
                    android:textStyle="bold"
                    android:textSize="13sp"
                    app:backgroundTint="@color/accent_purple"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_translate"
                    app:iconTint="@color/white" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <!-- Demo Mode Button (Hackathon) -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnDemoMode"
                    android:layout_width="wrap_content"
                    android:layout_height="48dp"
                    android:text="@string/action_demo"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:textSize="13sp"
                    app:backgroundTint="@color/accent_cyan"
                    app:cornerRadius="24dp"
                    app:icon="@drawable/ic_demo"
                    app:iconTint="@color/black" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="wrap_content" />

                <!-- Settings Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnSettings"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:insetLeft="0dp"
                    android:insetTop="0dp"
                    android:insetRight="0dp"
                    android:insetBottom="0dp"
                    app:cornerRadius="24dp"
                    app:backgroundTint="@color/surface_dark"
                    app:icon="@drawable/ic_settings"
                    app:iconTint="@color/white"
                    app:strokeColor="@color/surface_border"
                    app:strokeWidth="1dp"
                    android:contentDescription="@string/action_settings" />
            </LinearLayout>
        </HorizontalScrollView>
    </LinearLayout>

    <!-- Permission / Fallback Container -->
    <LinearLayout
        android:id="@+id/permissionContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:padding="24dp"
        android:background="@drawable/card_background"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@drawable/ic_camera"
            android:contentDescription="Camera required"
            app:tint="@color/accent_green" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/permission_title"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/permission_message"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:gravity="center" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGrantPermission"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="20dp"
            android:text="@string/grant_permission"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            app:backgroundTint="@color/accent_green"
            app:cornerRadius="12dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
